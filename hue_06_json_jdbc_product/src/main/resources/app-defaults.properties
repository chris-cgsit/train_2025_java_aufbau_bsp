#
#  Copyright Â© 2025 CGS IT Solutions GmbH.
#  All rights reserved.
#
#  This source code is proprietary intellectual property
#  of CGS IT Solutions GmbH and is provided solely for use
#  within licensed training programs.
#
#   Any copying, redistribution, modification, or use
#   beyond the permitted scope is strictly prohibited.
#

# ====================================================================
# Konfigurationsdatei fÃ¼r die Anwendung (Standardwerte / Default-Werte)
#
# Dateiname: app-defaults.properties (definiert in APP_CONFIG_FILENAME)
#
# Die Konfiguration wird in dieser Reihenfolge geladen (PrioritÃ¤t: Hoch -> Niedrig):
# 1. Kommandozeilen-Argumente (args)
# 2. Umgebungsvariablen (ENV)
# 3. Externe Properties-Datei (fileProps)
# 4. Standardwerte aus dieser Datei (defaultProps)
# 5. Hardcodierte Fallback-Werte (z.B. "jdbc:h2:./productdb")
# ====================================================================


# --------------------------------------------------------------------
# 1. Datenbank-Konfiguration
#
# Diese Werte dienen als Standard-Datenbankverbindung.
# Sie kÃ¶nnen Ã¼ber Umgebungsvariablen (DB_URL, DB_USER, DB_PASSWORD)
# oder Kommandozeilen-Argumente (dbUrl, dbUser, dbPassword) Ã¼berschrieben werden.
# --------------------------------------------------------------------

# Die JDBC-URL zur Datenbank (Standard: PostgresDB oder  / lokale H2-Datenbank-Datei)
# die datenbank selbst und ggf einen user bitte mit SQL script im sql folder anlegen
# dbUrl=jdbc:h2:./productdb
dbUrl=jdbc:postgresql://localhost:5432/product_importer_db
# Der Benutzername fÃ¼r die Datenbankverbindung (Standard: sa for h2 and postgres for postgres)
dbUser=postgres
# Das Passwort fÃ¼r die Datenbankverbindung (Standard: leer)
dbPassword=postgres

# --------------------------------------------------------------------
# 2. Anwendungs-Modus und Eingabe
#
# Definiert, in welchem Modus die Anwendung starten soll (z.B. list oder import).
# --------------------------------------------------------------------
# Der Anwendungsmodus (z.B. 'list' fÃ¼r Auflistung, 'import' fÃ¼r Datenimport)
# Kann Ã¼ber Umgebungsvariable APP_MODE Ã¼berschrieben werden.
# mode=list
# Der Pfad zur Eingabedatei (wird nur im 'import'-Modus benÃ¶tigt)
# stelle in der RUN configuration sicher, dass das aktuelle modul das start verzeichnis ist
# und nicht der Parent
input=data/products.json

# --------------------------------------------------------------------
# 3. Optionale Filter-Parameter
#
# Diese Parameter steuern die Filterung der Produkte bei der Auflistung.
# Sie sind optional und kÃ¶nnen null sein.
# --------------------------------------------------------------------
# Gibt an, ob nur aktive Produkte berÃ¼cksichtigt werden sollen (true/false)
# Kann Ã¼ber Umgebungsvariable ONLY_ACTIVE Ã¼berschrieben werden.
# Wird "null", wenn hier oder extern kein Wert gesetzt wird.
# onlyActive=true
# Der Mindestpreis fÃ¼r Produkte (als Dezimalzahl)
# Kann Ã¼ber Umgebungsvariable MIN_PRICE Ã¼berschrieben werden.
# Wird "null", wenn hier oder extern kein Wert gesetzt wird.
# minPrice=10.50


